export interface RegisterUserData {
  userFirstName: string;
  userLastName: string;
  email: string;
  password: string;
}

export interface LoginUserData {
  userEmail: string;
  userPassword: string;
}
